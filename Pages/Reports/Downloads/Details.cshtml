@page
@using AicaDocsUI.Extensions
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model AicaDocsUI.Pages.Reports.Downloads.Details

@{
    ViewData["Title"] = "Detalles de Descarga";
    var description = Model.DownloadDto.Format.GetDescription();
    var date = Model.DownloadDto.DateOfDownload.ToString("dd/MM/yyyy hh:mm:ss tt");
}


<div class="container">
    <div class="row justify-content-center">
        <div class="col-11 col-md-8 col-lg-5 mx-auto rounded border p-3">
            <h2 class="text-center">Detalles de Descarga</h2>
            <hr/>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <tbody>
                        <tr>
                            <th scope="row">@Html.Raw("Formato")</th>
                            <td>@Html.Raw(description)</td>
                        </tr>
                        <tr>
                            <th scope="row">@Html.Raw("Fecha de Descarga")</th>
                            <td>@Html.Raw(date)</td>
                        </tr>
                        <tr>
                            <th scope="row">@Html.Raw("Usuario")</th>
                            <td>@Html.DisplayFor(model => model.DownloadDto.UserEmail)</td>
                        </tr>
                        <tr>
                            <th scope="row">@Html.Raw("Documento")</th>
                            <td>@Html.DisplayFor(model => model.CodeEdition)</td>
                        </tr>
                        <tr>
                            <th scope="row">@Html.Raw("Fecha de Vigencia")</th>
                            <td>@Html.Raw(date)</td>
                        </tr>
                        <tr>
                            <th scope="row">@Html.Raw("Razón de descarga")</th>
                            <td>@Html.DisplayFor(model => model.Reason)</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <div class="text-start col">
                <a class="btn btn-outline-primary ms-auto w-25 w-sm-50" asp-page="./Index">Atrás</a>
            </div>
        </div>
    </div>
</div>